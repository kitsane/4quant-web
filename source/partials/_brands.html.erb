<div class="row m-b-1">
  <% collection.each do |brand| %>
    <div class="col-xs-6 col-sm-4 m-b-1">
      <%= image_tag "brands/#{collection_name}/#{brand.image}", alt: brand.name, class: 'img-fluid img-brand' %>
    </div>
  <% end %>
</div>
